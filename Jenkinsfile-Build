pipeline {
    agent { docker { image 'python:3' } }
    stages {
        stage('Build') {
            steps {
                withEnv(["HOME=${env.WORKSPACE}"]) {
                    sh 'python3 -m unittest discover -p "*_test.py"'
                }
            }
        }
    }
}